let person = {
    name : 'frengky',
    age : 24,
}
let array = Object.keys(person)
// array.map((val) => console.log(person[val])); // ini untuk hasil value
array.map((val) => console.log(val)); // ini untuk hasil keys(nama depan properti)

 
// for(let i in person)

// console.log(person[key])

 